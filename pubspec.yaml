name: firebase_write
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:  
  flutter:
    sdk: flutter
  provider:
  http:

  cupertino_icons: ^1.0.2  
  flutter_switch: ^0.3.2
  mask_text_input_formatter: ^2.4.0
  intl: ^0.17.0
  horizontal_data_table: ^4.1.4
  auto_size_text: ^3.0.0
  drag_and_drop_lists: ^0.3.1

  #firebase
  firebase_core: ^2.5.0
  cloud_firestore: ^4.3.2
  firebase_auth: ^4.2.6
  firebase_auth_web: ^5.2.5

  #scrollable_table_view: ^1.0.0  
  #string_validator: ^0.3.0
  #flutter_linear_datepicker: ^2.0.3
  #syncfusion_flutter_datepicker: ^20.4.43

  # teste
  font_awesome_flutter: ^10.3.0
  theme_mode_handler: ^3.0.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
